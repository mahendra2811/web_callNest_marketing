# APK Drop Folder

Place the production APK here as `callnest-latest.apk`. Update `src/content/releases.ts` with the matching version, size and SHA-256 checksum.

The placeholder file `callnest-latest.apk.placeholder` keeps this folder under version control until a real build is published.
